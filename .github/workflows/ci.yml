name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Check for unused code and dependencies
        run: npm run knip

      # Temporary: current tests treat absent default agent directories as
      # incomplete scans. Remove after SOURCE_SETUP_AND_MISSING_DIRECTORIES.md
      # is completed.
      - name: Create agent source directories required by current tests
        run: mkdir -p "$HOME/.claude/projects" "$HOME/.codex/sessions"

      - name: Test
        run: npm test

      - name: Inspect package contents
        run: npm pack --dry-run --ignore-scripts
